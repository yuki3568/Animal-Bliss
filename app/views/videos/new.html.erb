<div class="h-screen flex items-center justify-center">
  <div>
    <p class="mb-4 text-gray-600">癒されるおすすめの動物のshot動画を紹介しましょう！</p>
    <p class="mb-4 text-gray-600">動画のタイトルとURLを入力して投稿して下さい。</p>
    <%= form_with(model: @video, local: true) do |form| %>
      <div class="mb-3">
        <%= form.label :title %>
        <%= form.text_field :title, class: "form-control border border-gray-300 rounded" %>
      </div>
      <div class="mb-3">
        <%= form.label :url %>
        <%= form.text_field :url, class: "form-control border border-gray-300 rounded" %>
      </div>
      <div class="mb-3">
        <%= form.submit "投稿", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>
